<?xml version="1.0" encoding="UTF-8"?>
<assembly>
	<!-- jWebSocketGrizzlyEngine -->
	<!-- this id is appended to filename given in pom.xml -->
	<id>${project.version}</id>
	<formats>
		<format>jar</format>
	</formats>
	
	<includeBaseDirectory>false</includeBaseDirectory>
	
	<dependencySets>
		
		<dependencySet>
			<outputDirectory/>
			<includes>
				<include>${groupId}:${artifactId}</include>
				<include>org.glassfish.grizzly:grizzly-core</include>
				<include>org.glassfish.grizzly:grizzly-framework</include>
				<include>org.glassfish.grizzly:grizzly-websockets</include>
				<include>org.glassfish.grizzly:grizzly-websockets-server</include>
				
			</includes>
			<unpack>true</unpack>
		</dependencySet>		
		
	</dependencySets>
</assembly>